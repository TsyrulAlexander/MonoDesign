<UserControl x:Class="MonoDesign.UI.View.GameObjectListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" Width="250" DataContext="{Binding GameObjectList, Source={StaticResource Manager}}">
    <Grid>
        <ListBox ItemsSource="{Binding GameObjectList.Items}" SelectedItem="{Binding GameObjectList.CurrentItem}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock FontSize="14" Text="{Binding Path=GameObject.Name}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
