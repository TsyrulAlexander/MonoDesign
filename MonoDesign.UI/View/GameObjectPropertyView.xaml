<UserControl x:Class="MonoDesign.UI.View.GameObjectPropertyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:component="clr-namespace:MonoDesign.UI.View.Component"
             mc:Ignorable="d" 
             DataContext="{Binding GameObjectProperty, Source={StaticResource Manager}}">
    <Grid>
        <GroupBox>
            <GroupBox.HeaderTemplate>
                <DataTemplate>
                    <TextBlock VerticalAlignment="Center" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Text="{Binding GameObject.Name}" />
                </DataTemplate>
            </GroupBox.HeaderTemplate>
           <ListView ItemsSource="{Binding GameObject.Components}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <component:ComponentView Component="{Binding}"></component:ComponentView>
                </DataTemplate>
            </ListView.ItemTemplate>
            </ListView>
        </GroupBox>
    </Grid>
</UserControl>
